#!/bin/bash

# GitHub Issues 创建脚本
# 用于将 PRD 转换为 GitHub Issues 和 Milestones
# 项目：nav_blog UI 升级

set -e

echo "🚀 开始创建 GitHub Issues..."

# 检查 gh CLI 是否已认证
if ! gh auth status &>/dev/null; then
    echo "❌ GitHub CLI 未认证，请先运行: gh auth login"
    exit 1
fi

# 获取当前仓库信息
REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner)
OWNER=$(echo $REPO | cut -d'/' -f1)
REPO_NAME=$(echo $REPO | cut -d'/' -f2)
echo "📦 当前仓库: $REPO"

# ============================================
# 辅助函数：创建或获取 Milestone
# ============================================

create_or_get_milestone() {
    local title="$1"
    local description="$2"

    # 检查 milestone 是否已存在
    local existing_number=$(gh api "/repos/$REPO/milestones" --jq ".[] | select(.title == \"$title\") | .number" 2>/dev/null || echo "")

    if [ -n "$existing_number" ]; then
        echo "✓ Milestone 已存在: $title (编号: $existing_number)"
        echo "$existing_number"
    else
        # 创建新 milestone
        local response=$(gh api "/repos/$REPO/milestones" -X POST -f title="$title" -f description="$description" 2>&1)
        local new_number=$(echo "$response" | jq -r '.number' 2>/dev/null || echo "")

        if [ -n "$new_number" ] && [ "$new_number" != "null" ]; then
            echo "✅ 创建 Milestone: $title (编号: $new_number)"
            echo "$new_number"
        else
            echo "❌ 创建 Milestone 失败: $title"
            echo "错误信息: $response"
            echo ""
        fi
    fi
}

# ============================================
# 创建 Milestones
# ============================================

echo ""
echo "📌 创建 Milestones..."

# Phase 1: 视觉风格重构（MVP 核心）
MILESTONE1_TITLE="Phase 1: 视觉风格重构（MVP 核心）"
MILESTONE1_DESC="建立赛博朋克视觉基础，实现核心视觉升级。包括：赛博朋克配色系统、动态粒子星空背景、书签卡片重设计、3D 卡片悬浮效果、页面过渡动画。"
MILESTONE1_NUMBER=$(create_or_get_milestone "$MILESTONE1_TITLE" "$MILESTONE1_DESC")

# Phase 2: 交互增强与角色系统
MILESTONE2_TITLE="Phase 2: 交互增强与角色系统"
MILESTONE2_DESC="增加二次元元素和交互趣味性。包括：看板娘角色基础版、动漫风格图标和插画、看板娘高级功能、更多装饰元素。"
MILESTONE2_NUMBER=$(create_or_get_milestone "$MILESTONE2_TITLE" "$MILESTONE2_DESC")

# ============================================
# 创建 Issues - Phase 1
# ============================================

echo ""
echo "📝 创建 Phase 1 Issues..."

# Issue 1: 赛博朋克配色系统
gh issue create \
  --title "实现赛博朋克配色系统" \
  --body "$(cat <<'EOF'
## User Story

**作为** 二次元爱好者
**我想要** 看到深蓝、紫色、霓虹色调的赛博朋克配色方案
**以便** 界面能反映我的审美偏好，而不是千篇一律的商务风格

## 功能描述

实现完整的赛博朋克配色方案，包括深色基调、霓虹色强调、渐变效果和发光元素。

### 用户流程
1. 用户打开页面，立即看到深蓝/紫色的背景
2. 卡片、按钮、文字使用霓虹色（粉、青、紫）
3. 交互元素具有发光效果（hover 时增强）
4. 用户可在设置中切换暗色/亮色模式

### 边界情况
- 亮色模式下如何保持赛博朋克风格？→ 使用浅色背景 + 深色霓虹色
- 色盲用户如何识别？→ 确保足够的对比度，不仅依赖颜色区分

### 错误处理
- CSS 变量加载失败 → 回退到默认配色
- 主题切换失败 → 保持当前主题，显示错误提示

## Acceptance Criteria

- [ ] 主色调采用深蓝（#0a0e27）、紫色（#6366f1）、霓虹粉（#ec4899）、霓虹青（#06b6d4）
- [ ] 背景使用深色基调，避免刺眼的亮色
- [ ] 文字和图标使用高对比度的霓虹色，确保可读性
- [ ] 支持暗色模式（默认）和亮色模式切换
- [ ] 移动端保持相同的配色方案
- [ ] CSS 变量系统支持主题切换
- [ ] 所有现有组件应用新配色

## 技术要点

- 扩展 Tailwind CSS 配置文件
- 使用 CSS Variables 实现动态主题切换
- 确保 WCAG AA 级别的对比度标准

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 1, Story 1
EOF
)" \
  --label "feature,ui,phase-1,priority-high" \
  --milestone "$MILESTONE1_TITLE" 2>/dev/null && echo "✅ Issue 1: 实现赛博朋克配色系统" || echo "⚠️  Issue 1 可能已存在或创建失败"

# Issue 2: 动态粒子星空背景
gh issue create \
  --title "实现动态粒子星空背景" \
  --body "$(cat <<'EOF'
## User Story

**作为** 用户
**我想要** 看到动态的粒子星空背景效果
**以便** 界面更有科幻感和沉浸感

## 功能描述

使用 Canvas API 实现动态粒子星空背景，粒子缓慢移动并具有发光效果。

### 用户流程
1. 页面加载时初始化粒子系统
2. 粒子在背景层缓慢移动（速度可配置）
3. 鼠标移动时粒子有轻微的交互反应（可选）
4. 用户可在设置中关闭粒子效果（性能优化）

### 边界情况
- 低端设备性能不足 → 自动降级为静态渐变背景
- 用户禁用 JavaScript → 显示静态背景图片
- 浏览器不支持 Canvas → 优雅降级

### 错误处理
- Canvas 初始化失败 → 回退到 CSS 渐变背景
- 动画帧率低于 20fps → 自动减少粒子数量或禁用效果

## Acceptance Criteria

- [ ] 背景包含缓慢移动的粒子效果（模拟星空）
- [ ] 粒子数量在 50-100 个之间，确保性能
- [ ] 粒子具有渐变透明度和微弱的发光效果
- [ ] 鼠标移动时粒子有轻微的交互反应（可选）
- [ ] 移动端简化为静态渐变背景或减少粒子数量
- [ ] 动画帧率保持在 30fps 以上
- [ ] 使用 `requestAnimationFrame` 优化动画
- [ ] 粒子数量根据设备性能动态调整

## 技术要点

- 使用 Canvas API 或 React Spring
- 实现性能监测和自动降级
- 提供用户设置开关

## 性能要求

- 首次内容绘制（FCP）< 1.5 秒
- 动画帧率 ≥ 30fps（目标 60fps）
- CPU 占用 < 10%

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 1, Story 2
EOF
)" \
  --label "feature,ui,animation,phase-1,priority-high" \
  --milestone "$MILESTONE1_TITLE" 2>/dev/null && echo "✅ Issue 2: 实现动态粒子星空背景" || echo "⚠️  Issue 2 可能已存在或创建失败"

# Issue 3: 书签卡片重设计
gh issue create \
  --title "书签卡片赛博朋克风格重设计" \
  --body "$(cat <<'EOF'
## 功能描述

将现有书签卡片重新设计为赛博朋克风格，包括霓虹色边框、发光效果和悬停动画。

## Acceptance Criteria

- [ ] 卡片应用赛博朋克配色方案
- [ ] 霓虹色边框和发光效果
- [ ] 悬停时的基础动画（缩放、发光增强）
- [ ] 保持卡片内容清晰可读
- [ ] 移动端适配

## 技术要点

- 使用 Tailwind CSS 自定义样式
- CSS box-shadow 实现发光效果
- 简单的 CSS transition 实现悬停动画

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 1, 必须完成项 3
EOF
)" \
  --label "feature,ui,phase-1,priority-high" \
  --milestone "$MILESTONE1_TITLE" 2>/dev/null && echo "✅ Issue 3: 书签卡片赛博朋克风格重设计" || echo "⚠️  Issue 3 可能已存在或创建失败"

# Issue 4: 3D 卡片悬浮效果
gh issue create \
  --title "实现 3D 卡片悬浮和倾斜效果" \
  --body "$(cat <<'EOF'
## User Story

**作为** 用户
**我想要** 书签卡片具有 3D 悬浮和倾斜效果
**以便** 交互更有趣，视觉更立体

## 功能描述

使用 Framer Motion 实现书签卡片的 3D 倾斜、悬浮阴影和点击反馈动画。

### 用户流程
1. 用户鼠标悬停在卡片上
2. 卡片根据鼠标位置产生 3D 倾斜效果
3. 卡片显示悬浮阴影和发光边框
4. 用户点击卡片时产生缩放反馈
5. 卡片打开对应的书签链接

### 边界情况
- 触摸设备无鼠标悬停 → 简化为点击时的缩放效果
- 卡片内容过长 → 使用省略号，hover 时显示完整内容
- 快速连续点击 → 防抖处理，避免重复触发

### 错误处理
- Framer Motion 加载失败 → 回退到 CSS 过渡动画
- 3D 变换不支持 → 使用 2D 缩放效果

## Acceptance Criteria

- [ ] 鼠标悬停时卡片产生 3D 倾斜效果（跟随鼠标位置）
- [ ] 卡片具有悬浮阴影和发光边框
- [ ] 过渡动画流畅（使用 Framer Motion）
- [ ] 点击卡片时有缩放反馈动画
- [ ] 移动端简化为简单的缩放效果（无 3D 倾斜）
- [ ] 卡片内容（标题、图标）清晰可读

## 技术要点

- 安装 Framer Motion: `npm install framer-motion`
- 使用 `motion.div` 包装卡片组件
- 实现鼠标位置追踪和 3D 变换计算
- 提供降级方案

## 依赖

- 依赖 Issue #3（书签卡片重设计）完成

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 1, Story 3
- [Framer Motion 文档](https://www.framer.com/motion/)
EOF
)" \
  --label "feature,ui,animation,phase-1,enhancement" \
  --milestone "$MILESTONE1_TITLE" 2>/dev/null && echo "✅ Issue 4: 实现 3D 卡片悬浮和倾斜效果" || echo "⚠️  Issue 4 可能已存在或创建失败"

# Issue 5: 页面过渡动画
gh issue create \
  --title "实现页面和组件过渡动画" \
  --body "$(cat <<'EOF'
## 功能描述

为页面路由切换、组件进入/退出、加载状态添加流畅的过渡动画。

## Acceptance Criteria

- [ ] 路由切换时有淡入淡出动画
- [ ] 组件进入时有滑入或淡入动画
- [ ] 组件退出时有滑出或淡出动画
- [ ] 加载状态显示赛博朋克风格的 loading 动画
- [ ] 所有动画流畅，不影响性能

## 技术要点

- 使用 Framer Motion 的 `AnimatePresence`
- Next.js App Router 路由动画
- 自定义 loading 组件

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 1, 增强功能 5
EOF
)" \
  --label "feature,ui,animation,phase-1,enhancement" \
  --milestone "$MILESTONE1_TITLE" 2>/dev/null && echo "✅ Issue 5: 实现页面和组件过渡动画" || echo "⚠️  Issue 5 可能已存在或创建失败"

# ============================================
# 创建 Issues - Phase 2
# ============================================

echo ""
echo "📝 创建 Phase 2 Issues..."

# Issue 6: 动漫风格图标和插画
gh issue create \
  --title "设计和实现动漫风格图标和插画" \
  --body "$(cat <<'EOF'
## User Story

**作为** 用户
**我想要** 看到手绘风格的图标和装饰性插画元素
**以便** 界面更有二次元氛围

## 功能描述

重绘分类图标为手绘风格，添加装饰性插画元素，设计空状态页面插画。

## Acceptance Criteria

- [ ] 分类图标采用线条风格或手绘风格
- [ ] 页面角落添加装饰性插画元素（如几何图形、线条装饰）
- [ ] 空状态页面使用插画而非纯文字
- [ ] 图标和插画风格统一，符合赛博朋克主题
- [ ] 所有图标支持 SVG 格式，确保清晰度

## 技术要点

- 使用 Figma 或 Illustrator 设计图标
- 导出为 SVG 格式
- 可使用开源资源（unDraw, Storyset）或 AI 生成

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 2, Story 4
- [unDraw 插画库](https://undraw.co/)
- [Storyset 动画插画](https://storyset.com/)
EOF
)" \
  --label "feature,ui,design,phase-2" \
  --milestone "$MILESTONE2_TITLE" 2>/dev/null && echo "✅ Issue 6: 设计和实现动漫风格图标和插画" || echo "⚠️  Issue 6 可能已存在或创建失败"

# Issue 7: 看板娘角色基础版
gh issue create \
  --title "实现看板娘角色助手（基础版）" \
  --body "$(cat <<'EOF'
## User Story

**作为** 用户
**我想要** 一个可交互的看板娘角色
**以便** 界面更有趣，并能快速访问常用功能

## 功能描述

在页面右下角添加可交互的看板娘角色，提供动态表情、问候语和快捷功能入口。

### 用户流程
1. 页面加载后看板娘出现在右下角（带入场动画）
2. 用户点击看板娘，显示对话框和功能菜单
3. 看板娘根据时间段显示不同的问候语
4. 用户可通过看板娘快速访问搜索、添加书签、设置等功能
5. 用户可在设置中最小化或隐藏看板娘

### 边界情况
- 移动端屏幕小 → 缩小看板娘尺寸或仅显示图标
- 用户频繁点击 → 限制动画触发频率
- 看板娘遮挡内容 → 支持拖拽位置或自动避让

### 错误处理
- 看板娘资源加载失败 → 显示占位图标，功能菜单仍可用
- 动画库加载失败 → 使用静态图片 + CSS 动画

## Acceptance Criteria

### 基础版（必须完成）
- [ ] 看板娘固定在页面右下角或侧边
- [ ] 点击看板娘时显示对话框和功能菜单
- [ ] 基础表情变化（2-3 种状态）
- [ ] 快捷功能入口（搜索、添加书签、设置）
- [ ] 看板娘可以最小化或隐藏（用户偏好设置）
- [ ] 移动端看板娘缩小尺寸或仅显示图标

### 增强版（如时间允许）
- [ ] 根据时间段显示不同的问候语或表情
- [ ] 拖拽位置功能
- [ ] 更多表情和动作

## 技术要点

- 基础版：静态图片 + CSS 动画
- 增强版：考虑 Live2D 或 Spine 动画
- 使用 React Context 管理看板娘状态
- 实现拖拽功能（react-draggable）

## 设计资源

- 需要寻找或委托设计看板娘角色
- 可使用开源素材或 AI 生成
- 风格需符合赛博朋克主题

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 2, Story 5
- [Live2D Cubism SDK](https://www.live2d.com/)
EOF
)" \
  --label "feature,ui,animation,phase-2,priority-high" \
  --milestone "$MILESTONE2_TITLE" 2>/dev/null && echo "✅ Issue 7: 实现看板娘角色助手（基础版）" || echo "⚠️  Issue 7 可能已存在或创建失败"

# Issue 8: 看板娘高级功能
gh issue create \
  --title "看板娘高级功能（Live2D 动画）" \
  --body "$(cat <<'EOF'
## 功能描述

为看板娘添加 Live2D 动画支持，实现更丰富的表情和动作。

## Acceptance Criteria

- [ ] 集成 Live2D SDK
- [ ] 实现 Live2D 模型加载和渲染
- [ ] 更多表情和动作（5+ 种）
- [ ] 时间段问候语系统
- [ ] 拖拽位置功能
- [ ] 性能优化（不影响页面性能）

## 技术要点

- 研究 Live2D Cubism SDK 集成方案
- 如集成困难，使用 Spine 或 GIF 动画替代
- 实现模型资源懒加载

## 依赖

- 依赖 Issue #7（看板娘基础版）完成

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 2, 增强功能 3
EOF
)" \
  --label "feature,ui,animation,phase-2,enhancement" \
  --milestone "$MILESTONE2_TITLE" 2>/dev/null && echo "✅ Issue 8: 看板娘高级功能（Live2D 动画）" || echo "⚠️  Issue 8 可能已存在或创建失败"

# Issue 9: 更多装饰元素
gh issue create \
  --title "添加更多赛博朋克装饰元素" \
  --body "$(cat <<'EOF'
## 功能描述

为页面添加更多赛博朋克风格的装饰元素，提升视觉丰富度。

## Acceptance Criteria

- [ ] 页面角落的几何装饰
- [ ] 鼠标跟随光效
- [ ] 滚动视差效果
- [ ] 装饰元素不影响内容可读性
- [ ] 移动端简化或隐藏部分装饰

## 技术要点

- CSS 伪元素实现几何装饰
- JavaScript 实现鼠标跟随效果
- Intersection Observer 实现滚动视差

## 相关文档

- PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- Phase 2, 增强功能 4
EOF
)" \
  --label "feature,ui,animation,phase-2,enhancement" \
  --milestone "$MILESTONE2_TITLE" 2>/dev/null && echo "✅ Issue 9: 添加更多赛博朋克装饰元素" || echo "⚠️  Issue 9 可能已存在或创建失败"

# ============================================
# 创建 Epic Issue（可选）
# ============================================

echo ""
echo "📋 创建 Epic Issue..."

gh issue create \
  --title "🎨 赛博朋克风格 UI/UX 全面升级" \
  --body "$(cat <<'EOF'
## 项目概述

将现有的 Next.js 14 + Supabase 个人导航页从当前的"AI 风味"极简商务设计，全面升级为**赛博朋克风格的二次元主题导航站**。

## 核心价值

- 面向二次元爱好者社群
- 提供独特的赛博朋克美学体验
- 通过酷炫的视觉效果和有趣的交互设计，提升用户参与度和社交传播度

## 成功指标

### 主要 KPI
1. **性能指标**：页面首次加载时间 < 3 秒，动画流畅度 ≥ 30fps
2. **用户认可度**：正面反馈数量 ≥ 10 条
3. **用户参与度**：平均停留时间增加 50%

## 实施阶段

### Phase 1: 视觉风格重构（MVP 核心）
建立赛博朋克视觉基础，实现核心视觉升级。

**必须完成**：
- [ ] 实现赛博朋克配色系统
- [ ] 实现动态粒子星空背景
- [ ] 书签卡片赛博朋克风格重设计

**增强功能**（如时间允许）：
- [ ] 实现 3D 卡片悬浮和倾斜效果
- [ ] 实现页面和组件过渡动画

### Phase 2: 交互增强与角色系统
增加二次元元素和交互趣味性。

**必须完成**：
- [ ] 设计和实现动漫风格图标和插画
- [ ] 实现看板娘角色助手（基础版）

**增强功能**（如时间允许）：
- [ ] 看板娘高级功能（Live2D 动画）
- [ ] 添加更多赛博朋克装饰元素

## 技术栈

- **前端框架**：Next.js 14 (App Router), TypeScript, React 18
- **样式方案**：Tailwind CSS, CSS Variables, Shadcn/ui
- **动画库**：Framer Motion, Canvas API
- **兼容性**：Chrome 90+, Edge 90+, Safari 14+, Firefox 88+

## 风险与缓解

| 风险 | 缓解策略 |
|------|----------|
| 性能风险 | 渐进增强策略，低端设备自动降级 |
| 审美风险 | 保留主题切换功能，可回退到简洁模式 |
| 技术复杂度 | Phase 2 先实现基础版，Live2D 作为增强功能 |

## 相关文档

- 📄 PRD: `docs/ui-ux-upgrade-cyberpunk-prd.md`
- 📊 项目看板: [GitHub Project](项目链接)

## 进度追踪

使用 GitHub Milestones 追踪各阶段进度。

---

*本 Epic 由 PRD 自动生成，详细需求请参考各子 Issue。*
EOF
)" \
  --label "epic,phase-1,phase-2" \
  --milestone "$MILESTONE1_TITLE" 2>/dev/null && echo "✅ Epic Issue: 赛博朋克风格 UI/UX 全面升级" || echo "⚠️  Epic Issue 可能已存在或创建失败"

# ============================================
# 完成
# ============================================

echo ""
echo "🎉 所有 Issues 创建完成！"
echo ""
echo "📊 查看创建的 Issues:"
echo "   gh issue list"
echo ""
echo "🔗 在浏览器中查看:"
echo "   gh repo view --web"
echo ""
echo "📌 查看 Milestones:"
echo "   gh api /repos/$REPO/milestones --jq '.[] | \"\\(.number): \\(.title) (\\(.open_issues)/\\(.open_issues + .closed_issues))\"'"
