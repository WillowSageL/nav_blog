# 将 Issues 添加到 GitHub Project 的指南

## 方案 A: 使用 GitHub Web 界面（推荐）

这是最简单的方法，不需要额外的权限配置。

### 步骤 1: 创建 GitHub Project

1. 访问您的仓库: https://github.com/WillowSageL/nav_blog
2. 点击顶部的 **Projects** 标签
3. 点击 **New project** 按钮
4. 选择 **Board** 视图（看板视图）
5. 输入项目名称: `nav_blog UI 升级`
6. 点击 **Create**

### 步骤 2: 添加 Issues 到 Project

**方法 1: 批量添加（推荐）**
1. 在 Project 页面，点击右上角的 **+ Add item**
2. 在搜索框中输入 `#` 会显示所有 Issues
3. 选择所有相关的 Issues（#2-#11）
4. 它们会自动添加到 "Todo" 列

**方法 2: 从 Issue 页面添加**
1. 打开任意 Issue（如 #2）
2. 在右侧边栏找到 **Projects** 部分
3. 点击 **Add to project**
4. 选择 `nav_blog UI 升级` 项目
5. 重复此步骤添加其他 Issues

### 步骤 3: 组织 Project 看板

**创建自定义列**:
1. 点击列标题旁的 **+** 按钮
2. 创建以下列:
   - 📋 **Backlog** - 待规划
   - 🚀 **Todo** - 待开始
   - 🔨 **In Progress** - 进行中
   - 👀 **In Review** - 审查中
   - ✅ **Done** - 已完成

**按 Phase 分组**:
1. 点击右上角的 **View options** (三个点)
2. 选择 **Group by** → **Milestone**
3. 这样会按 Phase 1 和 Phase 2 分组显示

**按优先级排序**:
1. 点击 **View options**
2. 选择 **Sort by** → **Labels**
3. 高优先级的 Issues 会排在前面

---

## 方案 B: 使用 GitHub CLI（需要额外权限）

如果您想使用命令行，需要先配置权限。

### 步骤 1: 刷新 GitHub CLI 权限

```bash
# 复制一次性代码
gh auth refresh -h github.com -s project,read:project

# 会显示类似:
# ! First copy your one-time code: 8CAF-D37E
# Open this URL to continue in your web browser: https://github.com/login/device

# 1. 复制代码（如 8CAF-D37E）
# 2. 在浏览器中打开 https://github.com/login/device
# 3. 输入代码并授权
```

### 步骤 2: 运行脚本

```bash
cd /Users/v_liangjiawei02/Desktop/导航页
./add_issues_to_project.sh "nav_blog UI 升级"
```

---

## 方案 C: 使用 GitHub API（高级）

如果您有 Personal Access Token，可以使用 API：

```bash
# 1. 创建 Project
PROJECT_ID=$(gh api graphql -f query='
  mutation {
    createProjectV2(input: {
      ownerId: "YOUR_USER_ID"
      title: "nav_blog UI 升级"
    }) {
      projectV2 {
        id
        number
      }
    }
  }
' --jq '.data.createProjectV2.projectV2.id')

# 2. 添加 Issues
for issue_num in {2..11}; do
  gh api graphql -f query="
    mutation {
      addProjectV2ItemById(input: {
        projectId: \"$PROJECT_ID\"
        contentId: \"$(gh api /repos/WillowSageL/nav_blog/issues/$issue_num --jq .node_id)\"
      }) {
        item {
          id
        }
      }
    }
  "
done
```

---

## 推荐方案

**对于您的情况，我推荐使用方案 A（Web 界面）**，因为：
- ✅ 最简单，不需要额外配置
- ✅ 可视化操作，更直观
- ✅ 可以同时设置看板布局和分组
- ✅ 不需要处理权限问题

只需要 2-3 分钟就能完成！

---

## 快速链接

- 📦 仓库: https://github.com/WillowSageL/nav_blog
- 📋 Issues: https://github.com/WillowSageL/nav_blog/issues
- 🎯 创建 Project: https://github.com/WillowSageL/nav_blog/projects/new

---

## Project 看板建议布局

```
┌─────────────────────────────────────────────────────────────┐
│  nav_blog UI 升级                                            │
├─────────────┬─────────────┬─────────────┬─────────────┬─────┤
│  📋 Backlog │  🚀 Todo    │  🔨 In Prog │  👀 Review  │ ✅ Done│
├─────────────┼─────────────┼─────────────┼─────────────┼─────┤
│             │ Phase 1     │             │             │     │
│             │ ─────────── │             │             │     │
│             │ #2 配色系统  │             │             │     │
│             │ #3 粒子背景  │             │             │     │
│             │ #4 卡片设计  │             │             │     │
│             │ #5 3D效果   │             │             │     │
│             │ #6 过渡动画  │             │             │     │
│             │             │             │             │     │
│             │ Phase 2     │             │             │     │
│             │ ─────────── │             │             │     │
│             │ #7 图标插画  │             │             │     │
│             │ #8 看板娘    │             │             │     │
│             │ #9 Live2D   │             │             │     │
│             │ #10 装饰元素 │             │             │     │
└─────────────┴─────────────┴─────────────┴─────────────┴─────┘
```

开始开发时，将 Issue 从 Todo 拖到 In Progress，完成后拖到 Done！
